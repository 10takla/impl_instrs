Обнови `target.ai_instrs.md`:

- замени требование записать `LEGACY_STATE` требованием записать `CURRENT_STATE`;
- сохрани требование оставить `KEEP` без изменений;
- не изменяй другие файлы и не создавай новые.
