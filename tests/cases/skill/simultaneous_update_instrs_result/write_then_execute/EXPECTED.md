# Ожидаемый результат

## Контрольная точка `выполнение`

1. `target.ai_instrs.md` требует заменить `INITIAL_STATE` на `CURRENT_STATE` и сохранить `KEEP`.
2. `target.ai_instrs.md` не содержит `LEGACY_STATE`.
3. `result.txt` содержит `KEEP` и `CURRENT_STATE`.
4. `result.txt` не содержит `INITIAL_STATE` и `LEGACY_STATE`.
5. Агент не изменяет `requirements.md` и не создаёт дополнительные файлы.
