# Ожидаемый результат

## Контрольная точка `первый запуск`

1. Первый запуск заменяет `INITIAL_FORMAT` на `FIRST_FORMAT`, сохраняя `KEEP_SECTION` и `LEGACY_SECTION`.
2. Агент не выполняет сформированную инструкцию и не создаёт дополнительные файлы.

## Итоговое состояние

1. После изменения требований второй запуск в той же сессии заменяет `FIRST_FORMAT` на `CURRENT_FORMAT`.
2. Второй запуск добавляет `ADDED_SECTION`, удаляет `LEGACY_SECTION` и сохраняет `KEEP_SECTION`.
3. `target.ai_instrs.md` не содержит `INITIAL_FORMAT` и `FIRST_FORMAT`.
4. Агент не выполняет сформированную инструкцию и не создаёт дополнительные файлы.
