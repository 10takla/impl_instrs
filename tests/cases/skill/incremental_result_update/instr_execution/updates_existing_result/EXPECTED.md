# Ожидаемый результат

## Контрольная точка `первый запуск`

1. Первый запуск заменяет `INITIAL_STATE` на `FIRST_STATE`, сохраняя `KEEP` и `LEGACY_STATE`.

## Итоговое состояние

1. После изменения инструкции второй запуск в той же сессии заменяет `FIRST_STATE` на `CURRENT_STATE`.
2. Второй запуск добавляет `ADDED_STATE`, удаляет `LEGACY_STATE` и сохраняет `KEEP`.
3. `result.txt` не содержит `INITIAL_STATE` и `FIRST_STATE`.
4. `requirements.ai_instrs.md` содержит актуальные требования второго запуска.
5. Агент не создаёт дополнительные файлы.
