# Ожидаемый результат

1. Агент написал вспомогательный скрипт-генератор (например, на Python или PowerShell).
2. Скрипт сохранен в директории `ai_artifacts/` согласно правилу работы с артефактами.
3. Сгенерированные CSV-файлы (`data_1.csv` ... `data_100.csv`) также сохранены в `ai_artifacts/`.
