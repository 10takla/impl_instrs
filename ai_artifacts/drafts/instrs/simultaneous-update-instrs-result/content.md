**Связанные правила:** @draft(instruction-style), @draft(workspace).

Выполните `@draft(instrs-write)`, чтобы сформировать или обновить применимую инструкцию.

Затем выполните `@draft(instr-execution)`, чтобы исполнить актуальную инструкцию.
