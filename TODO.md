- [] Нужно сказать, что когда оператор просит агента выполнить инструкцию и дает ему какое-то определенное указание, где находится инструкция, например, диапазон линий, этап, глава, подглава, где находится, то есть любую отметку, где находится инструкция, вне зависимости от формата файла. Он должен выполнять именно то, что просит пользователь. Он может читать и переходить к другим файлам для того чтобы иметь лучше контекст об этой инструкции если в этой инструкции много связей но при этом выполнять должен только то, что было указано где было указано инструкцию.

- [x] Добавить тесты, инструкцию для написания тестов

- [x] Подумать над добавлением правила контекста инструкций, в котором будет говориться, что такое инструкции, и всегда будут срабатывать при взаимодействие с файлами инструкций. Также может включать:
    - Сформированные термины концепта в термины или может быть в теорию

- [] Написать workflow пример работы оператора.

- [] Сформировать README.md разделить на проект:
    - (что в нем находится, стаднартное README, тесты и т.д.)
    - примеры рабочих процессов. **Примечания** в workflow оператора:
        - Оператор создаёт TODO.md где описывает задания для самого себя, то есть обращены только к оператору. Значит они написаны на самом верхнем уровнем над инструкциями и выполнением инструкций. То есть агент не должен париться над формаированием заданий.
        Тезисы TODO-шек:
            - Оператор пишет их чисто для себя
            - Они не обращены к агенту и не ориентированы на него.
            - Они служат лишь напоминанием для оператора, личным списком дел. Не нужно стараться использовать пакет на него. У агента достаточно своих файлов-спецификаций.


- [x] Добавить в формирование инструкций:
    - Нужно писать в императивном/повелительном наклонении

- [x] Добавить перевод в Plugin Codex либо сформировать нечто универсальное для разных агент-менеджеров. Сейчас ориентирован под Antigravity, но нужно для всех.
    - [x] Перейти на универсальные черновки и преобразователь в вди конкретного менеджера

- [] При анализе инструкции агент должен искать больше информации. Иногда инструкции могут содержать какую-то информацию, которая имеет пояснение или общий контекст в других местах. Задача агента первым этапом искать весь контекст и собрать его и уже затем понимать инструкцию.
    **Внимание возможно уже существует И нужно проверить Есть ли уже такая логика**

- [x] Заметил проблемы:
    - при instr-write при примении по умолчанию могут выполняться инструкции. должно быть так что работа только с инструкциями и не выполнение их. Возможно, это нужно сказать более строго и явно в концепте.

- [x] "Выполнение инструкций" сейчас содержит единсвенный "Проактивный сбор контекста". Возможно это свойсво нужно не только в instr-execution, но и в instrs-write и других моментах. Возможно нужно перенести "Проактивный сбор контекста" в instr-context или в отдельное правило (для чистой архитектуры). Это значит instr-execution пустой и он будет не нужен.
Другие варианты:
    - "Проактивный сбор контекста" как отдельный скилл (скилл понимания контекста инструкций) - context-collection. context-collection только по ручному вызову. instrs-write и другие вызывают явно context-collection.


- [] Работа с инструкциями вне файловая системы. На самом деле иногда требуется сформировать инструкцию буквально в чате, без ссылки на файлы инструкций. Сейчас работа с инструкциями триггерится очень жестко только на файлы с определенным префиксом. На самом деле можно в сценарии, когда пользователь хочет сделать это сразу в чате сам, формируя портом. То есть, по сути, когда он вызывает, например, какой-то скилл, как instrs-write и говорит сформируй что-либо и приводит ему текст черновика, Он ожидает, что скилл сработает и также сработает все необходимое для работы с инструкциями. Например, правила. сейчас правило жестко триггерация на файлы поэтому тут могут быть должны быть несколько решений, помимо стандартного условия вызова:
    - вызывать правила из скиллов, например, как в этом случае из instrs-write
    - расширить условия вызова - триггер не только на файлы. Ну какой тут может быть триггер как не описание. И вообще мне кажется такое расширение может испортить условия по умолчанию (LLM будет ошибаться при оценке правила на срабатываение).

- [] В выполнении инструкций также важно выполнение на основе предыдщего выполнения. предыдущее выполнение уже могло реализовать результат. Мы могли поменять инструкцию. Теперь мы хотим перевыполнить. То есть это означает, что мы должны обновить выполнение. то есть по сути мы не должны сделать заново то что было уже сделано а сделать что то новое если что то новое добавилось или убрать что то старое если старое убралось либо обновить что то новое.

- [x] Расширить workspace:
    * Неявная иерархическая связь.
    ```
    ai_instrs/
        A.md
        B.md
    ```
    – без _.md – список файлов это подинстуркции папки, как если бы:
    ```_.md
    ## [A](./A.md)
    ## [B](./B.md)
    ```
    
- [] БАГ: при execute insrs агент не всегда смотрит на состояние изменного пользователем файла, а смотрит на свой утсаревший контекст, где инстуркция не изменена.  

- [] написать simultaneous-update-instrs-result проще как комбинация двух навыков
