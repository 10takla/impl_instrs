## Формат путей

При упоминании или формировании путей к файлам и папкам абсолютные пути запрещены.

Используются исключительно относительные пути:
- относительный путь от текущего расположения
- относительный путь от целевого файла или инструкции

---

## Файловая модель инструкций

<!-- TODO: Также, возможно, нужно обдумать мета-инструкции -->

Инструкции обнаруживаются по паттернам имён файлов и папок:

**Папки (группы инструкций):**
- `<keyword>/`
- `<название>.<keyword>/`
- `<keyword>.<название>/`

Все файлы внутри такой папки являются инструкциями.

Файл `_.md` наследует имя папки и является родительской инструкцией. Если он отсутствует, иерархия считается неявной: имя папки сохраняет родительский уровень, а остальные файлы остаются подинструкциями.

**Файлы (одиночные инструкции):**
- `<keyword>.<расширение>`
- `<название>.<keyword>.<расширение>`

Где `<keyword>` — "ai_instrs".

---

## Артефакты и побочные файлы

Любые файлы, скрипты, отчеты или иные артефакты, генерируемые агентом, для которых в инструкциях не указано конкретное место сохранения, фиксируются в директории `./ai_artifacts/` относительно, по приоритету: файла инструкции, рабочей директории
