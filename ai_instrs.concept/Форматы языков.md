md:
- Пути к файлам []() только через (<>) {{Как это называется?}}? чтобы сохранить пробелы
- 
    ```
    trigger: glob
    globs: `**/*.md`
    ```

### Скилл – короткий ответ:

**Содержимое:**
```
При генерации развернутого ответа, в самый конец сообщения необходимо добавлять его краткое лаконичное резюме. 
Если ответ изначально короткий, ничего добавлять не нужно. 
Данное правило не должно влиять на содержание, стиль и процесс формирования самого ответа.
```

Должен вызываться строго при / команде опертором, также как в [](<./Структура пака.md#явный-вызов-оператором>).

Расположение: [](</ai_artifacts/skills/>)
Перевод на английский: [](</skills/>)